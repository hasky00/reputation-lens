{
  "name": "reputation-lens",
  "version": "0.1.0",
  "private": true,
  "description": "X API reputation and spam-risk context panel.",
  "scripts": {
    "build": "vite",
    "check": "node --check src/main.js && node --check lib/reputation.js && node --check api/scan.js && node --check api/x-health.js"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
