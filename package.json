{
  "name": "reputation-lens",
  "version": "0.1.0",
  "private": true,
  "description": "X API reputation and spam-risk context panel.",
  "scripts": {
    "check": "node --check public/app.js && node --check lib/reputation.js && node --check api/scan.js && node --check api/x-health.js"
  },
  "engines": {
    "node": ">=20"
  }
}
